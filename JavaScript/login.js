const form = document.getElementById('form1');
const usernameInput = document.getElementById('username');
const passwordInput = document.getElementById('password');
const loginButton = document.getElementById('button3');

// Function to check if the form is valid
// function isFormValid() {
//   return usernameInput.value.trim() !== '' && passwordInput.value.trim() !== '';
// }

// // Function to enable or disable the login button based on form validity
// function toggleLoginButton() {
//   loginButton.disabled = !isFormValid();
// }

// // Call the toggleLoginButton function on page load
// toggleLoginButton();

// // Call the toggleLoginButton function on input change
// form.addEventListener('input', toggleLoginButton);

// if(usernameInput==null||passwordInput==null)
// {
// //   loginButton.disabled=true;
//   loginButton.ariaDisabled=true;
// }
// else{
//     loginButton.disabled=true;
// }